# 本项目专用 npm 配置：把缓存收进仓库内，安装/打包不会往工作区外写东西。
#
# 如果 npm install 时下载 Electron 很慢（国内常见），在安装前设置镜像即可：
#   PowerShell:  $env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
#   bash:        export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
# 注意用环境变量而不是写进 .npmrc —— @electron/get 读的是 ELECTRON_MIRROR。
cache=.npm-cache
